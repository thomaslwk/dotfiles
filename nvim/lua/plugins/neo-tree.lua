-- NeoTree open right side

return {
  "nvim-neo-tree/neo-tree.nvim",
  opts = {
    window = {
      position = "right", -- Set NeoTree to open on the right
      width = 35, -- Optional: Adjust the width of the NeoTree pane
    },
  },
}
