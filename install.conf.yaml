- defaults:
    link:
      relink: true

- clean: ['~']

- link:
   ~/.zshrc: zshrc
   ~/.tmux.conf: tmux.conf
   ~/.vimrc: vimrc
   ~/.gitconfig: gitconfig

- create:
    - ~/Projects

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
